% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/data-prep.R
\name{prep_uka_paired}
\alias{prep_uka_paired}
\title{Reshape a raw UKA table for the paired (golden) analysis, keyed by cell line}
\usage{
prep_uka_paired(uka, spec_cutoff, control, cs = NULL)
}
\arguments{
\item{uka}{Raw UKA data frame, Tercen-style dotted column names.}

\item{spec_cutoff}{Minimum specificity-score to keep a row.}

\item{control}{Name of the control condition as it appears in \code{contrast}
(e.g. \code{"DMSO"}). Set this to whichever side of every comparison is the
reference; the other side becomes \code{cell_line}.}

\item{cs}{\code{TRUE}/\code{FALSE} to force per-comparison (csUKA) vs. mean/median
columns; \code{NULL} (default) auto-detects via \code{\link[networkGen:detect_csuka]{networkGen::detect_csuka()}}.}
}
\value{
Data frame with columns \code{cell_line}, \code{comparison} (the full,
untruncated "X vs control"/"control vs X" label -- \code{cell_line} itself
has \code{control} stripped out, since it's used as a join key against
sensitivity data's own cell-line identifier, not shown to the user),
\code{uniprotname}, \code{LogFC}, \code{fscore}.
}
\description{
The paired counterpart of \code{networkGen::prep_uka()}. Given a fixed
\code{control}, it keeps only the comparisons that involve that control
(either "X vs control" or "control vs X"), flips the kinase statistic's
sign when the control is on the left so \code{LogFC} always reads "treatment
relative to control", and derives \code{cell_line} -- the non-control side of
each comparison -- as the join key against the sensitivity data. Any
comparison not involving \code{control} is dropped.
}
