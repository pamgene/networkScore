% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/data-prep.R
\name{uka_top_kinase}
\alias{uka_top_kinase}
\title{Filter kinase-activity data to its top hits for the kinase-only analysis}
\usage{
uka_top_kinase(uka, spec_cutoff, rank_uka_abs = TRUE, perc_cutoff, cs = FALSE)
}
\arguments{
\item{uka}{Data frame with column \code{uniprotname}, \code{LogFC}, and either
\code{"Specificity Score"} or \code{"Mean Specificity Score"} (see \code{cs}).}

\item{spec_cutoff}{Minimum specificity score to keep a row.}

\item{rank_uka_abs}{If \code{TRUE} (default), rank by \code{abs(LogFC)}; if \code{FALSE},
rank by signed \code{LogFC}.}

\item{perc_cutoff}{Minimum percentile rank (0-1) to keep a row.}

\item{cs}{If \code{TRUE}, use \code{"Specificity Score"}; if \code{FALSE} (default), use
\code{"Mean Specificity Score"}.}
}
\value{
Data frame with columns \code{name}, \code{prize}, \code{type} ("Kinase"), \code{LogFC}.
}
\description{
Sibling of \code{networkGen::uka_top()}; filters by \code{spec_cutoff} on the
comparison-selected specificity column directly (rather than a
pre-normalized \code{fscore} column), then delegates to
\code{networkGen::percentile_score_fast()}/\code{percentile_score_noabs()}.
}
