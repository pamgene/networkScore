% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/data-prep.R
\name{clean_uka_to_kinograte_kinase}
\alias{clean_uka_to_kinograte_kinase}
\title{Clean a raw UKA table for the kinase-only analysis, keyed by condition}
\usage{
clean_uka_to_kinograte_kinase(
  uka,
  cs = NULL,
  condition_col = "Sgroup_contrast"
)
}
\arguments{
\item{uka}{Raw UKA data frame, Tercen-style dotted column names.}

\item{cs}{\code{TRUE}/\code{FALSE} to force per-comparison (csUKA) vs. mean/median
columns; \code{NULL} (default) auto-detects via \code{\link[networkGen:detect_csuka]{networkGen::detect_csuka()}}.}

\item{condition_col}{Name of the raw column identifying each condition/
comparison, carried through unchanged to the output. This is the same
role \code{Sgroup_contrast} plays in \code{networkGen::clean_uka_to_kinograte()};
different Tercen exports name it differently (\code{"Sgroup_contrast"},
\code{"Sample"}, ...). Default \code{"Sgroup_contrast"}. Must match the
\code{condition_col} passed to \code{networkGen::build_network_grid()}.}
}
\value{
Data frame with columns \verb{<condition_col>}, \code{uniprotname},
\code{LogFC}, \code{fscore}.
}
\description{
The kinase-only counterpart of \code{networkGen::clean_uka_to_kinograte()}:
same reduction of a raw Tercen UKA export to the shape the grid builder
consumes, differing only in which raw column identifies each condition
(\code{condition_col}).
}
