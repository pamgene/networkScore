% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/stats.R
\name{compute_network_stats}
\alias{compute_network_stats}
\title{Compute network-topology statistics for a scored network}
\usage{
compute_network_stats(network_result, a = 1)
}
\arguments{
\item{network_result}{A \code{"networkGen_result"} object (from
\code{networkGen::generate_paired_network()} / \code{generate_kinase_network()}).}

\item{a}{Smoothing constant in the inverse shortest-path transform
(\code{1 / (a + distance)}). Default 1.}
}
\value{
A list: \code{rel_med_path} (median shortest-path length across all
node pairs, relative to node count -- lower means a tighter network),
\code{rel_med_path_inv} (same, inverse-transformed so higher means tighter),
\code{density}, \code{clustering} (mean local transitivity).
}
\description{
The statistics used to compare an observed network against its permuted
null distribution. This is the reconciled replacement for
\code{Network_generation}'s \code{compute_network_metrics_kinase()} (the only one of
the two original statistics functions that was ever actually implemented
-- the paired-data variant was commented out and never finished) --
ported unchanged in substance, used for both the paired and kinase-only
scoring paths (both build a \code{networkGen_result}, and this function only
looks at its \code{network}/\code{missing_nodes}, so one implementation serves
both).
}
\details{
\code{modularity}/\code{assortativity} (present as placeholder fields elsewhere in
the pre-migration code, never actually computed) and the \verb{_core}/\verb{_core_inv}
fields the old permutation code read but nothing computed are deliberately
not reproduced here -- see \verb{docs/adr/} at the \code{DevOpti} root.
}
