% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/results.R
\name{drop_constant_grid_columns}
\alias{drop_constant_grid_columns}
\title{Drop grid-parameter columns that don't actually vary across a result set}
\usage{
drop_constant_grid_columns(
  results,
  grid_cols = c("spec_cutoff", "perc_cutoff", "b", "rank_uka_abs", "ppi_network")
)
}
\arguments{
\item{results}{A results data frame (e.g. from
\code{\link[=make_golden_score_kinase]{make_golden_score_kinase()}}/\code{\link[=make_golden_score_full]{make_golden_score_full()}}, after
combining every folder's rows).}

\item{grid_cols}{Character vector of column names to consider dropping
if constant. Default the five \code{networkGen}/\code{networkScore} grid
dimensions.}
}
\value{
\code{results}, with any of \code{grid_cols} removed if it has at most one
distinct (non-missing) value across all rows.
}
\description{
\code{make_golden_score_kinase()}/\code{make_golden_score_full()} record all five
grid dimensions (\code{spec_cutoff}, \code{perc_cutoff}, \code{b}, \code{rank_uka_abs},
\code{ppi_network}) on every row, regardless of whether a particular call
actually gridded that dimension -- most calls fix most of them at a
single value. A column that's constant across every row is noise, not
information about what varies in \emph{this} result; this keeps only the
ones that actually differ somewhere in the combined result, so the
columns shown depend on the grid actually run, not a fixed schema.
}
\details{
Only ever drops from the columns it's told to consider (\code{grid_cols}) --
\code{Comparison} and every score/stat column are always kept, whether or not
this is called on them.
}
