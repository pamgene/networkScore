% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/network-score.R
\name{make_golden_score_full}
\alias{make_golden_score_full}
\title{Full (paired kinase + sensitivity) golden score analysis}
\usage{
make_golden_score_full(
  uka,
  sens,
  control,
  spec_cutoff,
  perc_cutoff,
  respath,
  uka_fam,
  ppi_network,
  b,
  del_cells = NULL,
  zscore = FALSE,
  best_drug_per_target = NULL,
  score_overlap = TRUE,
  score_network = TRUE,
  nperms_overlap = 500,
  nperms_network = 50,
  rank_uka_abs = TRUE,
  balance = FALSE,
  cs = FALSE,
  max_tasks = 500,
  ...
)
}
\arguments{
\item{uka}{Raw UKA data frame, Tercen-style dotted column names.}

\item{sens}{Raw sensitivity data frame.}

\item{control}{Control condition name, as it appears in UKA's \code{contrast} column.}

\item{spec_cutoff, perc_cutoff, b, rank_uka_abs}{Vectors -- every combination
is scored, not just the ones a particular caller happens to compare.}

\item{respath}{Base output directory. Each combination gets its own
parameter-encoded subfolder under it (see \code{\link[=prepare_score_run_params]{prepare_score_run_params()}})
-- \code{results.csv}, \code{metrics_permutations.csv}, \code{metrics_observed.csv},
the faceted diagnostic histograms, and each cell's observed network
(\code{nodes_*.csv}/\code{edges_*.csv}/\code{wc_df_*.csv}, not permutation networks)
all land there.}

\item{uka_fam}{Kinase-family lookup table (\code{Kinase_Name}, \code{Kinase_family}), for overlap scoring.}

\item{ppi_network}{A data frame with columns \code{head}, \code{tail}, \code{cost}, or a
fully named list of them (e.g. \code{list(v12 = ppi_networkv12, kins502 = ppi_networkv12_502_kins)}) to also grid across more than one reference
network.}

\item{del_cells}{Optional character vector of cell lines to exclude.}

\item{zscore}{If \code{TRUE}, sensitivity is z-scored across cell lines instead
of fold-change vs. \code{control}.}

\item{best_drug_per_target}{Optional data frame restricting sensitivity to
one drug per target.}

\item{score_overlap, score_network}{If \code{FALSE}, skip that score entirely
(fields left \code{NA}). Both default \code{TRUE}.}

\item{nperms_overlap, nperms_network}{Number of permutations for each score. Defaults 500, 50.}

\item{balance}{If \code{TRUE}, lowers the sensitivity percentile cutoff by 0.2 (see \code{networkGen::sens_top()}).}

\item{cs}{If \code{TRUE}, use the per-comparison specificity column.}

\item{max_tasks}{Refuse to proceed (\code{stop()}, without building anything)
if the grid, with permutations, expands to more than this many PCSF
builds -- a safety guard against an unintentionally huge overnight run,
not a time estimate (overlap-score permutations are cheap, no PCSF
calls, and don't count toward this). Raise explicitly if the size is
actually intended. Default 500.}

\item{...}{Passed to every network build, uniform across the whole grid
(not gridded) -- e.g. \code{n}, \code{w}, \code{r}, \code{mu}, \code{seed}.}
}
\value{
A list: \code{results} (one row per \code{Comparison} x spec_cutoff x
perc_cutoff x b x rank_uka_abs x ppi_network, across every
combination's folder -- \code{Comparison} is the full, untruncated
"X vs control" contrast label, e.g. \code{"DrugA vs Control"}; the
cell-line-only join key used internally against sensitivity data isn't
part of the output), \code{logs}.
}
\description{
Unified grid interface, same shape as \code{\link[=make_golden_score_kinase]{make_golden_score_kinase()}}:
\code{spec_cutoff}/\code{perc_cutoff} are gridded via
\code{\link[networkGen:build_network_grid]{networkGen::build_network_grid()}}, cells are then restricted to cell
lines with matching sensitivity data. \code{sens_filt} depends on \verb{(cell, perc_cutoff)} only (sensitivity has no specificity-cutoff concept), so
it's resolved once per cell after the kinase-side grid is built, not
gridded independently.
}
