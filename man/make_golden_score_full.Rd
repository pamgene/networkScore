% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/network-score.R
\name{make_golden_score_full}
\alias{make_golden_score_full}
\title{Full (paired kinase + sensitivity) golden score analysis}
\usage{
make_golden_score_full(
  uka,
  sens,
  control,
  spec_cutoff,
  respath,
  uka_fam,
  perc_cutoffs,
  ppi_network,
  b,
  del_cells = NULL,
  zscore = FALSE,
  best_drug_per_target = NULL,
  score_overlap = TRUE,
  score_network = TRUE,
  nperms_overlap = 500,
  nperms_network = 50,
  rank_uka_abs = TRUE,
  balance = FALSE,
  cs = FALSE
)
}
\arguments{
\item{uka}{Raw UKA data frame, Tercen-style dotted column names.}

\item{sens}{Raw sensitivity data frame.}

\item{control}{Control condition name, as it appears in UKA's \code{contrast} column.}

\item{spec_cutoff}{Specificity-score cutoff.}

\item{respath}{Results folder.}

\item{uka_fam}{Kinase-family lookup table (\code{Kinase_Name}, \code{Kinase_family}), for overlap scoring.}

\item{perc_cutoffs}{Vector of percentile cutoffs to score at.}

\item{ppi_network}{Data frame with columns \code{head}, \code{tail}, \code{cost}.}

\item{b}{PCSF terminal-prize weight.}

\item{del_cells}{Optional character vector of cell lines to exclude.}

\item{zscore}{If \code{TRUE}, sensitivity is z-scored across cell lines instead
of fold-change vs. \code{control}.}

\item{best_drug_per_target}{Optional data frame restricting sensitivity to
one drug per target.}

\item{score_overlap, score_network}{If \code{FALSE}, skip that score entirely
(fields left \code{NA}). Both default \code{TRUE}.}

\item{nperms_overlap, nperms_network}{Number of permutations for each score. Defaults 500, 50.}

\item{rank_uka_abs}{If \code{TRUE} (default), rank kinase hits by \code{abs(LogFC)}.}

\item{balance}{If \code{TRUE}, lowers the sensitivity percentile cutoff by 0.2 (see \code{networkGen::sens_top()}).}

\item{cs}{If \code{TRUE}, use the per-comparison specificity column.}
}
\value{
A list: \code{results} (one row per cell x perc_cutoff), \code{logs}.
}
\description{
Full (paired kinase + sensitivity) golden score analysis
}
