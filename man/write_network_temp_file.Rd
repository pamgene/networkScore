% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/network-score.R
\name{write_network_temp_file}
\alias{write_network_temp_file}
\title{Write a condition's network-score temp progress file}
\usage{
write_network_temp_file(respath, condition, perc_cutoff, vals)
}
\arguments{
\item{respath}{Folder to write into.}

\item{condition}{Condition/cell-line label.}

\item{perc_cutoff}{Percentile cutoff, for the file name (via \code{\link[=perc_suffix]{perc_suffix()}}).}

\item{vals}{A condition's result-row values, from \code{score_conditions()}'s
per-condition \code{vals} (must include \code{score_sig_network}/\code{score_sig_network_inv}).}
}
\value{
The path written, invisibly.
}
\description{
Shared by \code{\link[=make_golden_score_kinase]{make_golden_score_kinase()}} / \code{\link[=make_golden_score_full]{make_golden_score_full()}},
called once per condition right after \code{\link[=score_conditions]{score_conditions()}} returns.
Mirrors the \verb{temp_overlap_*} files \code{\link[=compute_golden_overlap_scores]{compute_golden_overlap_scores()}}
writes -- deleted by \code{\link[=finalize_golden_score_results]{finalize_golden_score_results()}} once the whole run
completes; lets a crashed/interrupted run be resumed.
}
\keyword{internal}
