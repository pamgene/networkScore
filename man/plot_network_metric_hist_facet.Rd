% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot.R
\name{plot_network_metric_hist_facet}
\alias{plot_network_metric_hist_facet}
\title{Faceted permutation-null-distribution diagnostic plot}
\usage{
plot_network_metric_hist_facet(all_metrics_df, all_obs_df, res.path)
}
\arguments{
\item{all_metrics_df}{Long-format permutation-value rows (\code{metric},
\code{perc_cutoff}, \code{value}, and a \code{cell} or \code{condition} grouping column).}

\item{all_obs_df}{Long-format observed-value rows, same shape.}

\item{res.path}{Folder to write \verb{Network_metrics_plots/<metric>_cutoff<cutoff>.png} into.}
}
\value{
Invisibly, the list of \code{ggplot} objects produced (also saved to disk).
}
\description{
One PNG per \code{metric} x \code{perc_cutoff} combination, faceted by condition:
each subplot is a histogram of that condition's permutation values for
that metric, with the observed value marked by a vertical line. Lets you
scan many conditions at once for which ones have their observed value out
in the tail (significant) vs. buried in the null distribution.
}
\details{
This is a scoring diagnostic, not network visualization -- it belongs
here in \code{networkScore}, not in \code{networkPlot}. Migrated from
\code{Network_generation}'s \code{R/golden_score_helper.R}, which already
generalized the original per-project (kinase-only vs. paired) split by
detecting whether the input has a \code{cell} or \code{condition} grouping column;
kept as-is here.
}
