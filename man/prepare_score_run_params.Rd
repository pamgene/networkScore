% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/params.R
\name{prepare_score_run_params}
\alias{prepare_score_run_params}
\title{Capture a scoring run's parameters, build its output folder, and write \code{params.csv}}
\usage{
prepare_score_run_params(...)
}
\arguments{
\item{...}{Passed to \code{\link[networkGen:capture_params]{networkGen::capture_params()}}. Must include
\code{respath} (the base output directory this run's parameter-encoded
folder is created under) and whatever \code{\link[=build_score_param_folder]{build_score_param_folder()}}
needs.}
}
\value{
The evaluated parameters as a named list, with \code{respath} replaced
by the created parameter-encoded folder path.
}
\description{
Composes \code{\link[networkGen:capture_params]{networkGen::capture_params()}} and \code{\link[=build_score_param_folder]{build_score_param_folder()}},
then the filesystem side effects -- mirrors
\code{\link[networkGen:prepare_run_params]{networkGen::prepare_run_params()}} but uses this package's extended
naming rule instead of \code{networkGen}'s alone. Used by
\code{\link[=make_golden_score_kinase]{make_golden_score_kinase()}} / \code{\link[=make_golden_score_full]{make_golden_score_full()}}.
}
