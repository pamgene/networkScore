% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils.R
\name{perc_suffix}
\alias{perc_suffix}
\title{Build the \verb{_<perc_cutoff>} temp-file suffix, omitted when there's no filter}
\usage{
perc_suffix(perc_cutoff)
}
\arguments{
\item{perc_cutoff}{The percentile cutoff used for this run.

Vectorized: safe to use on a single value or a vector of them (see
\code{\link[=find_missing_golden_score_combinations]{find_missing_golden_score_combinations()}}).}
}
\value{
\code{""} where \code{perc_cutoff} is \code{0}; \code{"_<perc_cutoff>"} elsewhere.
}
\description{
\code{perc_cutoff = 0} means "no percentile filtering" -- encoding it in every
temp-file name would just be noise, so it's dropped entirely in that case.
Used both when writing temp files and when looking them back up (see
\code{\link[=reconstruct_golden_score_from_temp]{reconstruct_golden_score_from_temp()}}/\code{\link[=find_missing_golden_score_combinations]{find_missing_golden_score_combinations()}})
-- both sides must apply the same rule or they'll disagree on the filename.
}
\keyword{internal}
