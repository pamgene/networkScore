% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/data-prep.R
\name{clean_sens_to_kinograte}
\alias{clean_sens_to_kinograte}
\title{Clean sensitivity data for the paired (golden) analysis}
\usage{
clean_sens_to_kinograte(
  sens,
  control,
  zscore = FALSE,
  del_cell = NULL,
  best_drug_per_target = NULL
)
}
\arguments{
\item{sens}{Raw sensitivity data frame with columns \code{CELL_LINE_NAME},
\code{TARGET_1}, \code{LN_IC50} (and \code{DRUG_ID} if \code{best_drug_per_target} is used).}

\item{control}{If given, \code{LogFC} is fold-change vs. this \code{cell_line}. If
\code{NULL} and \code{zscore = FALSE}, \code{sens} is returned as-is. If \code{NULL} and
\code{zscore = TRUE}, a per-target z-score across cell lines is computed.}

\item{zscore}{See \code{control}.}

\item{del_cell}{Optional character vector of \code{cell_line} values to drop.}

\item{best_drug_per_target}{Optional data frame with a \code{DRUG_ID} column;
if given, \code{sens} is restricted to rows whose \code{DRUG_ID} is in it.}
}
\value{
Data frame with columns \code{cell_line}, \code{uniprotname}, \code{LogFC}.
}
\description{
Sibling of \code{networkGen::clean_sens_to_kinograte()}; this variant renames
\code{CELL_LINE_NAME} (rather than expecting a pre-named \code{cell_line} column)
and can additionally restrict to a preferred drug per target.
}
