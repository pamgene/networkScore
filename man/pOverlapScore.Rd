% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/overlap.R
\name{pOverlapScore}
\alias{pOverlapScore}
\title{Permutation significance for the (family-wise) kinase/sensitivity overlap}
\usage{
pOverlapScore(
  score_obs,
  uka_cell_all,
  sens_filt,
  cell,
  nsample,
  nPerms = 1000,
  spec_cutoff,
  rank_uka_abs,
  perc_cutoff,
  uka_fam = NULL,
  family_mode = FALSE
)
}
\arguments{
\item{score_obs}{The observed overlap score (from \code{networkGen::overlap_uka_sens()}).}

\item{uka_cell_all}{All kinase-activity rows for this condition (unfiltered).}

\item{sens_filt}{Top-hit sensitivity data frame for this condition.}

\item{cell}{Condition/cell-line label, for logging.}

\item{nsample}{Unused, kept for call-site compatibility.}

\item{nPerms}{Number of permutations. Default 1000.}

\item{spec_cutoff, rank_uka_abs, perc_cutoff}{Passed to \code{networkGen::uka_top()}.}

\item{uka_fam}{Kinase-family lookup table (\code{Kinase_Name}, \code{Kinase_family}
columns), required when \code{family_mode = TRUE}.}

\item{family_mode}{If \code{TRUE}, compute the overlap at kinase-family level
instead of individual-kinase level.}
}
\value{
The significance score: the fraction of permutations whose
overlap was at least as large as \code{score_obs} (floored at \code{1 / nPerms}).
}
\description{
Repeatedly shuffles kinase-activity values across kinases, re-selects top
hits, and recomputes the overlap with the sensitivity hits, to build a
null distribution for \code{score_obs}. Cheap relative to network-based
scoring (no PCSF calls), so this stays sequential rather than going
through \code{networkGen::generate_networks_batch()}.
}
